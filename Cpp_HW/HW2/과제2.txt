1. 기한 내에 블랙보드에 업로드할 할당을 완료합니다.
2. 이름과 학생증을 분명히 언급하세요.
3. 다른 친구로부터 복사하지 마세요. 발견 시 0마크가 부여됩니다.
4. 빈 파일은 제출하지 마십시오. 발견하면 벌점을 받는다.
5. 모든 문제를 단일 파일(.docx)로 제출합니다.
6. 파일에는 각 질문에 대한 출력 화면과 함께 소스 코드가 포함되어야 합니다.
7. 코멘트 옵션을 사용하여 코드를 설명하십시오.
8. 정답은 질문에 정확하게 일치해야 합니다.
9. 궁금한 점이 있으면 언제든지 편지함을 보내 주세요.

Q.1) 클래스 개념으로 인라인 함수를 사용하여 세 숫자 중 가장 큰 숫자를 찾습니다. 
두 개의 기능 input()과 display()를 선택합니다. 
인라인 함수는 가장 큰 값을 계산한 후 값을 반환해야 합니다. 
개체를 크게 정의합니다.

Q.2) calc라는 클래스를 만들고 5개의 멤버 함수 이름을 
get(), sum(), difference(), product(), division()으로 만듭니다. 
또한 개체 obj1을 생성합니다. 
인라인 함수 개념을 적용하여 프로그램을 코드화합니다.

Q.3) Q1)에서 매개 변수화된 생성자를 사용하여 객체 big에 대해 
10, 100, 300으로 변수를 초기화하는 생성자 개념을 구현하고, 멤버 함수를 사용하여 
런타임에 새로운 데이터 집합으로 초기화된 값을 덮어쓰고 
세 가지 중에서 가장 큰 값을 계산해야 합니다.

Q.4) 아래와 같이 3개의 생성자를 정의하기 위한 생성자 과부하 개념을 설명하기 위한 
코드를 작성한다. 인수가 있는 생성자의 경우 개체로 전달된 인수를 표시합니다.
●인수가 2개인 생성자 1개
●인수가 1개인 생성자 1개
●인수가 없는 생성자 1명(메시지 표시 가능)
및 아래에 지정된 대로 3개의 개체를 만듭니다.
●인수가 2개인 객체 1개
●인수가 1개인 객체 1개
인수가 없는 하나의 객체

Q.5) 클래스를 사용하여 세 숫자 중 가장 큰 숫자를 찾습니다. 
생성자 함수 input()을 생성하여 런타임에 객체를 초기화하고 멤버 함수를
 largest()로 생성하여 가장 큰 값을 계산하고 결과를 표시합니다. 
개체 big를 사용하여 이러한 멤버 함수를 호출했습니다.

참고: 별도의 컴파일 개념을 사용합니다. 
별도의 헤더 파일(.h), 기본 파일(.cpp) 및 클래스 파일(.cpp)을 생성합니다.
지침:
3개의 파일의 내용을 따로따로 넣으시오.
1.h 파일
2. 주. cpp 파일
3. Class.cpp
